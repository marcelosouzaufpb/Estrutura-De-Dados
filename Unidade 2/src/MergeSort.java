
public class MergeSort<Item extends Comparable<Item>> {
	
/*	public void ordena(Item[] vetor, int indiceInicio, int indiceFim){
	// Condicional que verifica a validade dos parâmetros passados.
 		if (vetor != null && indiceInicio < indiceFim && indiceInicio >= 0 &&
 			indiceFim < vetor.length && vetor.length != 0) {
 			int meio = ((indiceFim + indiceInicio) / 2);
 			
 			ordena(vetor, indiceInicio, meio);
 			ordena(vetor, meio + 1, indiceFim);
		
 			merge(vetor, indiceInicio, meio, indiceFim);
 		}
		 		
	}
	
	public void particiona(Item[] vetor, int indInicio, int indMeio, int indFinal){
		
	}
	
	public void merge(Item[] vetor, int indiceInicio, int indiceMeio, int indiceFim){


	}*/
	public static void main(String [] args){
		String f = "CecyPaulaJosé";
		System.out.println(f.hashCode());
	}
	
}

